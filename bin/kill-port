#!/bin/sh

# Kills the process running on the provided port
#
# kill-port 3000

if [ -n "$1" ]; then
  port_num="$(lsof -ti4TCP:"$1")"
  if [ $? -eq 0 ]; then
    kill "$port_num"
  fi
else
  echo >&2 Usage: kill-port port-number
  exit 1
fi